SELECT 
    FRCD,
    PARTNO,
	ROUTECD,
	PARTCUSTID,
    SALEQTY AS QTY,
    CONVERT(DATE, CRTDT) AS DATE,
    CONVERT(TIME(0), CRTDT) AS TIME
FROM VHPMTPICKING
WHERE CRTDT >= '20250701' AND CRTDT < '20250710'

UNION ALL

SELECT 
    FRCD,
    PARTNO,
	ROUTECD,
	PARTCUSTID,
    SALEQTY AS QTY,
    CONVERT(DATE, CRTDT) AS DATE,
    CONVERT(TIME(0), CRTDT) AS TIME
FROM VPMTPICKING
WHERE CRTDT >= '20250701' AND CRTDT < '20250710'